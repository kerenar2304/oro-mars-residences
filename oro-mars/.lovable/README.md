# Lovable-ready components

Identical to `../OroMarsExperience.tsx` and `../TheCollection.tsx`, except every asset
path is rewritten to a `CDN` constant that points at this repository through jsDelivr.
Use these when the app is hosted somewhere that does not carry the 96MB of frames.
